/**
 * @file
 * Admin JavaScript for PropertySearch.
 */

(function (Drupal) {
  'use strict';

  /**
   * Admin initialization.
   */
  Drupal.behaviors.psAdmin = {
    attach: function (context, settings) {
      // Admin behaviors can be added here.
    },
  };
})(Drupal);
