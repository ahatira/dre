# Property Search multisite — Drush site aliases.
# Generated from scripts/multisite/countries.yml — run: make generate-multisite (repo root)
# uri = site directory under web/sites/ (not a full HTTP URL).

com:
  root: ../web
  uri: com
be:
  root: ../web
  uri: belgium
es:
  root: ../web
  uri: spain
fr:
  root: ../web
  uri: france
ie:
  root: ../web
  uri: ireland
it:
  root: ../web
  uri: italy
lu:
  root: ../web
  uri: luxembourg
nl:
  root: ../web
  uri: netherlands
pl:
  root: ../web
  uri: poland
