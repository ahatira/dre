{#
/**
 * @file
 * Primary CTA button + optional fallback URL line.
 *
 * Variables:
 * - button_url (required)
 * - button_label (required)
 * - button_show_fallback (optional, default true)
 * - email_primary_color (optional)
 */
#}
{% set green = email_primary_color|default('#00915a') %}
{% set text = email_text_color|default('#333333') %}
{% set surface = email_surface_color|default('#ffffff') %}
{% set url = button_url|default('') %}
{% set label = button_label|default('') %}

{% if url and label %}
  <mj-section background-color="{{ surface }}" padding="0 16px 24px">
    <mj-column>
      <mj-button href="{{ url }}" background-color="{{ green }}" color="#ffffff" font-size="13px" font-weight="700" letter-spacing="0.06em" text-transform="uppercase" border-radius="0" inner-padding="14px 28px">
        {{ label }}
      </mj-button>
      {% if button_show_fallback|default(true) %}
        <mj-text color="{{ text }}" font-size="13px" padding-top="16px">
          {{ 'If the button does not work, copy the following URL into your browser:'|t }}<br />
          <a href="{{ url }}" style="color:{{ green }};text-decoration:none;word-break:break-all;">{{ url }}</a>
        </mj-text>
      {% endif %}
    </mj-column>
  </mj-section>
{% endif %}
