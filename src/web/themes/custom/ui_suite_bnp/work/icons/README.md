# Icons

Place raw SVG files here.

Build commands:

- `npm run build:icons:svgo`: optimize SVG into `assets/icons/custom`.
- `npm run build:icons`: optimize + refresh `assets/icons/custom-icons.generated.yml`.
