name: Search hero
description: "Homepage hero shell — background, title, search, delegate and promo slots."
group: Property Search
status: stable
props:
  type: object
  properties:
    title:
      type: string
      title: Hero title
    background_image:
      type: string
      title: Hero background image URL
    background_alt:
      type: string
      title: Hero background alt text
slots:
  search:
    title: Search form
    description: Left column search form slot
  delegate:
    title: Delegate bar
    description: Delegate search prompt below the form
  promo:
    title: Promo panel
    description: Right column promotional panel
libraryOverrides:
  css:
    component:
      styles/search-hero.css: {}
