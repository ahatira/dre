{#
/**
 * @file
 * Wrapper — delegates to ps_theme SDC search-filter-range (Phase 5A.4).
 *
 * Variables: section, budget_config (budget only), capacity_filter_label (capacity only),
 * id_suffix (string).
 */
#}
{% include 'ps_theme:search-filter-range' with {
  section: section,
  id_suffix: id_suffix|default(''),
  capacity_filter_label: capacity_filter_label|default('Capacity'|t),
  capacity_config: capacity_config|default({}),
  budget_config: budget_config|default({}),
} only %}
