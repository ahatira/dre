{#
/**
 * @file
 * Wrapper — delegates to ps_theme SDC search-filter-type (Phase 5A.2).
 *
 * Variables: asset_types, operation_types, active_flexible.
 */
#}
{% include 'ps_theme:search-filter-type' with {
  asset_types: asset_types|default([]),
  operation_types: operation_types|default([]),
  active_flexible: active_flexible|default(true),
} only %}
