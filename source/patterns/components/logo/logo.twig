{#
/**
 * @file
 * PS Logo (Component/Molecule) - Brand logo for BNP Paribas Real Estate
 *
 * Flexible, data-driven logo component with automatic layout.
 * Inspired by Drupal's block--system-branding-block template.
 *
 * Available variables:
 * - site_logo: Logo image path or URI
 * - site_slogan: Site/company slogan (optional)
 * - url: Link URL (optional, typically <front>)
 * - rel: Link relationship (optional, e.g., "home")
 * - attributes: HTML attributes
 *
 * Auto-layout based on provided variables:
 * - If url → wrap in <a> tag
 * - If site_logo → display image
 * - If site_slogan → display slogan (centered)
 */
#}

{%- set has_content = site_logo or site_slogan -%}

{%- if has_content -%}
  {%- if url -%}
    <a href="{{ url }}" {{ attributes }}>
  {%- else -%}
    <div{{ attributes }}>
  {%- endif -%}

    {%- if site_logo -%}
      <div class="ps-logo__wrapper">
        <img class="ps-logo__image" src="{{ site_logo }}" alt="Logo" />
      </div>
    {%- endif -%}

    {%- if site_slogan -%}
      <div class="ps-logo__slogan">{{ site_slogan }}</div>
    {%- endif -%}

  {%- if url -%}
    </a>
  {%- else -%}
    </div>
  {%- endif -%}
{%- endif -%}

