{#
/**
 * Offer Hero Section (Gallery Carousel)
 * 
 * @param object gallery - Carousel configuration with slides
 * @param boolean skeleton - Loading state
 */
#}

{% set gallery = gallery|default({}) %}
{% set skeleton = skeleton|default(false) %}

{% if gallery.slides|default([])|length > 0 %}
  <section class="offer-hero" aria-label="Property gallery">
    {# Favoris button #}
    <button class="offer-hero__favorite" type="button" aria-label="Add to favorites" data-icon="heart" aria-pressed="false"></button>
    
    {% if skeleton %}
      {% include '@elements/skeleton/skeleton.twig' with {
        variant: 'rectangle',
        width: '100%',
        height: '500px'
      } only %}
    {% else %}
      {% include '@components/carousel/carousel.twig' with gallery only %}
    {% endif %}
  </section>
{% endif %}
